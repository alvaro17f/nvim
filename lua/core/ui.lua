------------------------------------
-- UI
------------------------------------
local ui = require("vim._extui")

ui.enable({
  enable = true,
  msg = {
    target = "msg",
    timeout = 1200,
  },
})
